<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.antonchaynikov.triptracker">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.location.gps"/>
    <uses-feature android:name="android.hardware.location.network"/>

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <uses-library android:name="android.test.runner"
            android:required="false" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".trips.TripsListActivity"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".mainscreen.TripActivity"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".authentication.LaunchActivity"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".trips.AbcActivity" />
        <activity android:name=".history.HistoryActivity" />

        <service android:name=".data.location.PlatformLocationService" />
        <service android:name=".data.location.LocationService" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="addf454c130e9713afedaadfdba64d014c511f73" />

    </application>
</manifest>
